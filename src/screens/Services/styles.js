import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1, 
        marginTop: 25,
        justifyContent: 'center', 
        alignItems: 'center',
    },
    anuncios: {
        height: 600,
        width: 380,
    },
    anuncio: {
        padding: 10,
        borderWidth: 1,
        borderColor: 'black',
    }
})