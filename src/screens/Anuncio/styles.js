import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1, 
        justifyContent: 'center', 
        alignItems: 'center',
    },
    anuncios: {
        height: 550,
        width: 350,
    },
    anuncio: {
        padding: 10,
        borderWidth: 1,
        borderColor: 'black',
        padding: 50,
    }
})